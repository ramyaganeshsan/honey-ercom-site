/node_modules
/logs

# Keep design assets tracked (CSS backgrounds / icons). Do not blanket-ignore images.
# Large optional dumps can still be ignored individually if needed later.

.env
dump.rdb
cloud/
